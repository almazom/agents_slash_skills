interface:
  display_name: "DocuTranslate"
  short_description: "Operate the local docutranslate / dt wrapper and its profiles"
  default_prompt: "Use $docutranslate to operate the local DocuTranslate wrapper in /home/pets/TOOLS/docutranslate_cli_skill, not the upstream repo. Prefer the global docutranslate or dt CLI, keep /home/pets/zoo/docutranslate read-only, and use the repo memory before changing configs or glossary."
